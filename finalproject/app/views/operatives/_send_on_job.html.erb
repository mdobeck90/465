<%= form_for([@user,@operative]) do |f| %>
  <% if @operative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operative.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @operative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field(:job_id, :value => Job.all.sample.id) %><br>
  </div>

  <div class="actions">
    <%= f.submit "Start Job",  {:controller => "operative", :action => "update" , :method => :post} %>
  </div>
  
<% end %>
