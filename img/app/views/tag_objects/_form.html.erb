<%= form_for(@tag_object) do |f| %>
  <% if @tag_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_object.errors.count, "error") %> prohibited this tag_object from being saved:</h2>

      <ul>
      <% @tag_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :str %><br>
    <%= f.text_field :str %>
  </div>
  <div class="field">
    <%= f.label :image_id %><br>
    <%= f.number_field :image_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
