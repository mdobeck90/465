<%= form_for([@image,@image_user]) do |f| %>
  <% if @image_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_user.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
      <% @image_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @image.private == true %>
    <p><strong>Add User to Access List</strong></p>
    <div class="field">
      <%= select_tag "Users", options_for_select(@userlist) %>
      <%= f.hidden_field(:image_user, :value => "Test") %>
    </div>

    <div class="actions">
      <%= f.submit "Add User" %>
    </div>
  <% end %>

<% end %>
