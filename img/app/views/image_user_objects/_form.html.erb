<%= form_for(@image_user_object) do |f| %>
  <% if @image_user_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_user_object.errors.count, "error") %> prohibited this image_user_object from being saved:</h2>

      <ul>
      <% @image_user_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_id %><br>
    <%= f.number_field :image_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
